/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { GoogleLoginButtonComponent } from './google-login-button.directive';

describe('Directive: GoogleLoginButton', () => {
  it('should create an instance', () => {
    const directive = new GoogleLoginButtonComponent();
    expect(directive).toBeTruthy();
  });
});
